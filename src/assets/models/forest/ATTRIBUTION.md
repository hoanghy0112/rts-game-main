# Forest Assets

Forest plant scenes in `plants/` and tree far-proxy scenes in `proxies/` are generated from user-provided temporary source packages copied into `source/`.

## User-Provided Sources

- `res://assets/models/forest/source/trees/canopy-tree/canopy_tree.glb`
- `res://assets/models/forest/source/grass/flower-grass/flower_grass.glb`
- `res://assets/models/forest/source/trees/highland-pine/highland_pine.glb`

The original temporary source folders are left untouched and are not referenced by project resources.
